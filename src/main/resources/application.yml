server:
  port: 8080

spring:
  application:
    name: kitchen-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://sh-cynosdbmysql-grp-a8qbs4n2.sql.tencentcdb.com:22806/springboot_demo?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:G8YTzppS}
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 微信小程序配置
wechat:
  miniapp:
    appid: wx8e54aabc053cd3cf
    secret: 738c206852857b8c255f4c96b3018d19

jwt:
  secret: ${JWT_SECRET:KitchenSecretKey2026JWTSecret}
  expiration: ${JWT_EXPIRATION:86400}

# AES对称加密配置
aes:
  key: KitchenAesKey2026

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:8080}

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

logging:
  level:
    com.kitchen: debug
